# Copy this file to .env.local and paste your real Google Maps API key.
# .env.local is git-ignored, so the key never gets committed.

GOOGLE_MAPS_API_KEY=paste_your_key_here

# Free alternative — sign up at https://locationiq.com (no card required)
LOCATIONIQ_TOKEN=paste_your_locationiq_token_here
